.PHONY: execute eval main

all: execute eval main
binary: execute eval main
opt: execute eval main
compd: execute eval main
match: execute eval main

execute:
	@echo
	${MAKE} -C execute $(MAKECMDGOALS)
eval:
	@echo
	${MAKE} -C eval $(MAKECMDGOALS)
main:
	@echo
	${MAKE} -C main $(MAKECMDGOALS)
