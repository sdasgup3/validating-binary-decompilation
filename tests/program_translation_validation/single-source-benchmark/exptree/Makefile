.PHONY: newWorkList newCombList initWorkList initCombList saveSolution calculate printSolution printList recSearch doSearch getInput search main

all: newWorkList newCombList initWorkList initCombList saveSolution calculate printSolution printList recSearch doSearch getInput search main
binary: newWorkList newCombList initWorkList initCombList saveSolution calculate printSolution printList recSearch doSearch getInput search main
opt: newWorkList newCombList initWorkList initCombList saveSolution calculate printSolution printList recSearch doSearch getInput search main
compd: newWorkList newCombList initWorkList initCombList saveSolution calculate printSolution printList recSearch doSearch getInput search main
match: newWorkList newCombList initWorkList initCombList saveSolution calculate printSolution printList recSearch doSearch getInput search main

newWorkList:
	@echo
	${MAKE} -C newWorkList $(MAKECMDGOALS)
newCombList:
	@echo
	${MAKE} -C newCombList $(MAKECMDGOALS)
initWorkList:
	@echo
	${MAKE} -C initWorkList $(MAKECMDGOALS)
initCombList:
	@echo
	${MAKE} -C initCombList $(MAKECMDGOALS)
saveSolution:
	@echo
	${MAKE} -C saveSolution $(MAKECMDGOALS)
calculate:
	@echo
	${MAKE} -C calculate $(MAKECMDGOALS)
printSolution:
	@echo
	${MAKE} -C printSolution $(MAKECMDGOALS)
printList:
	@echo
	${MAKE} -C printList $(MAKECMDGOALS)
recSearch:
	@echo
	${MAKE} -C recSearch $(MAKECMDGOALS)
doSearch:
	@echo
	${MAKE} -C doSearch $(MAKECMDGOALS)
getInput:
	@echo
	${MAKE} -C getInput $(MAKECMDGOALS)
search:
	@echo
	${MAKE} -C search $(MAKECMDGOALS)
main:
	@echo
	${MAKE} -C main $(MAKECMDGOALS)
