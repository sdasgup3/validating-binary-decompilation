.PHONY: main find pboard

all: main find pboard
binary: main find pboard
opt: main find pboard
compd: main find pboard
match: main find pboard

main:
	@echo
	${MAKE} -C main $(MAKECMDGOALS)
find:
	@echo
	${MAKE} -C find $(MAKECMDGOALS)
pboard:
	@echo
	${MAKE} -C pboard $(MAKECMDGOALS)
